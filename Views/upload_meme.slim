form action="uploading_meme" method="POST" enctype="multipart/form-data"
    input type="file" name="img" accept="image/*" required="required" onchange="loadFile(event)"
    input type="submit" value="Uppload!"


/ img id="output"

/ script
/     var loadFile = function(event)
/     var output = document.getElementById('output');
/     output.src = URL.createObjectURL(event.target.files[0]);
 